/* Hello World */
console.log('Hello World');

/* Exibindo na tela a soma de dois valores */
console.log(1+1);

/* criando três variáveis, variável a com o valor (1), variável b com o valor (2) e variável
result somando os valores que existem em (a e b), usando console.log para exibir as
somas pegando a variável (result) */
var a = 1;
var b = 2;
var result = a+b;
console.log(result);